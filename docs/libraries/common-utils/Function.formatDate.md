# Function: formatDate()

> **formatDate**(`date`, `format`): `string`

Format một date theo định dạng cho trước

## Parameters

### date

`Date`

### format

`string` = `'YYYY-MM-DD'`

## Returns

`string`
