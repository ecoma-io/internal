# Function: uniqueBy()

> **uniqueBy**\<`T`\>(`array`, `key`): `T`[]

Lấy các phần tử duy nhất từ một array dựa trên một key

## Type Parameters

### T

`T`

## Parameters

### array

`T`[]

### key

keyof `T`

## Returns

`T`[]
